{
  "version": 2,
  "builds": [
    {
      "src": "public/**/*",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/public/$1" },
    { "src": "/api/updatePredictions", "dest": "/api/updatePredictions.js" }
  ],
  "crons": [
    {
      "schedule": "30 6 * * *",  // Runs every day at 6:30 AM
      "url": "/api/updatePredictions"
    }
  ]
}
